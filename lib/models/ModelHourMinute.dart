class ModelHourMinute {
  int hour;
  int minute;
  get getHour => this.hour;

  set setHour(hour) => this.hour = hour;

  get getMinute => this.minute;

  set setMinute(minute) => this.minute = minute;
  ModelHourMinute(this.hour, this.minute);
}
